<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UI.Views.Import.ImportEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:UI.Utils.Converters" 
    mc:Ignorable="d">

    <Page.Resources>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>
        <converters:StatusColorConverter x:Key="StatusColorConverter"/>
        <converters:StatusTextConverter x:Key="StatusTextConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right" Background="Transparent" Margin="0,0,0,16" HorizontalAlignment="Stretch">
            <CommandBar.Content>
                <Button Content="Quay lại" Background="Transparent" BorderThickness="0" Click="OnBackButtonClicked">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE72B;" FontSize="14"/>
                                <TextBlock Text="Quay lại danh sách"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </CommandBar.Content>

            <AppBarButton Icon="Accept" Label="Hoàn thành phiếu" 
                          Foreground="SeaGreen"
                          Visibility="{x:Bind ViewModel.IsDraft, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                          Command="{x:Bind ViewModel.CompleteImportCommand}"/>

            <AppBarButton Icon="Delete" Label="Xóa phiếu tạm" 
                          Foreground="Red"
                          Visibility="{x:Bind ViewModel.IsDraft, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                          Command="{x:Bind ViewModel.DeleteImportCommand}"/>

            <AppBarButton Icon="Print" Label="Xuất PDF"
                          Command="{x:Bind ViewModel.ExportPdfCommand}"/>
        </CommandBar>

        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                CornerRadius="8" Padding="20" Margin="0,0,0,16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="MÃ PHIẾU" FontSize="12" Foreground="Gray" FontWeight="SemiBold"/>
                    <TextBlock Text="{x:Bind ViewModel.ImportIdText, Mode=OneWay}" FontSize="18" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="4" Margin="48,0,0,0">
                    <TextBlock Text="NGÀY LẬP" FontSize="12" Foreground="Gray" FontWeight="SemiBold"/>
                    <TextBlock Text="{x:Bind ViewModel.CreatedAtText, Mode=OneWay}" FontSize="16"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Spacing="4" HorizontalAlignment="Right">
                    <TextBlock Text="TRẠNG THÁI" FontSize="12" Foreground="Gray" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                    <Border Background="{x:Bind ViewModel.StatusText, Mode=OneWay, Converter={StaticResource StatusColorConverter}}" 
                            CornerRadius="4" Padding="8,4">
                        <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay, Converter={StaticResource StatusTextConverter}}" 
                                   Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <controls:DataGrid 
            Grid.Row="2"
            ItemsSource="{x:Bind ViewModel.Details, Mode=OneWay}"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            GridLinesVisibility="Horizontal"
            RowHeight="100" >
            <controls:DataGrid.Columns>
                <controls:DataGridTemplateColumn Header="Sản phẩm" Width="4*">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid x:Name="ProductCellRoot" Padding="8" ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        Width="80" Height="80" 
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Background="{ThemeResource SystemControlBackgroundAltHighBrush}">

                                    <Image Source="{Binding ProductImageUrl}"
                                           Stretch="UniformToFill"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           AutomationProperties.Name="{Binding ProductName}"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                    <TextBlock Text="{Binding ProductName}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               TextWrapping="Wrap"
                                               MaxLines="2"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding ProductSku}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <controls:DataGridTextColumn Header="Số lượng" Binding="{Binding QuantityAdded}" Width="1*">
                    <controls:DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </controls:DataGridTextColumn.ElementStyle>
                </controls:DataGridTextColumn>

                <controls:DataGridTextColumn Header="Giá nhập" 
                    Binding="{Binding ActualImportPrice, Converter={StaticResource StringFormatConverter}, ConverterParameter='\{0:N0\} đ'}" Width="1.5*">
                    <controls:DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </controls:DataGridTextColumn.ElementStyle>
                </controls:DataGridTextColumn>

                <controls:DataGridTextColumn Header="Thành tiền" 
                    Binding="{Binding TotalPrice, Converter={StaticResource StringFormatConverter}, ConverterParameter='\{0:N0\} đ'}" Width="1.5*">
                    <controls:DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </controls:DataGridTextColumn.ElementStyle>
                </controls:DataGridTextColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
