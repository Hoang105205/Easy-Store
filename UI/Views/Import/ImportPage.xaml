<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UI.Views.Import.ImportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI.Views.Import"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:UI.Utils.Converters.ImportPage"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>
        <converters:StatusColorConverter x:Key="StatusColorConverter"/>
        <converters:StatusIconConverter x:Key="StatusIconConverter"/>
        <converters:StatusTextConverter x:Key="StatusTextConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,24">
            <TextBlock Text="Lịch sử nhập hàng" Style="{StaticResource TitleTextBlockStyle}" FontWeight="SemiBold"/>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="TỔNG SỐ PHIẾU" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalCountText, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="TỔNG TIỀN ĐÃ NHẬP" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalAmountText, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" Foreground="SeaGreen"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="PHIẾU ĐANG LƯU TẠM" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{x:Bind ViewModel.DraftCountText, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" Foreground="DarkOrange"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <AutoSuggestBox PlaceholderText="Tìm kiếm mã phiếu..." Width="300" HorizontalAlignment="Left" VerticalAlignment="Center">
                <AutoSuggestBox.QueryIcon>
                    <SymbolIcon Symbol="Find"/>
                </AutoSuggestBox.QueryIcon>
            </AutoSuggestBox>

            <CommandBar DefaultLabelPosition="Right" Background="Transparent" HorizontalAlignment="Right">
                <AppBarButton Icon="Add" Label="Tạo phiếu nhập" ToolTipService.ToolTip="Nhập hàng thủ công" Click="OnCreateNewImportClicked"/>
                <AppBarButton Icon="Document" Label="Nhập từ Excel">
                    <AppBarButton.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="Tải file mẫu (.xlsx)" Icon="Download" Click="OnDownloadTemplateClicked"/>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem Text="Tải file lên để nhập" Icon="Upload" Click="OnUploadExcelClicked"/>
                        </MenuFlyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
                <CommandBar.SecondaryCommands>
                    <AppBarButton Icon="Refresh" Label="Làm mới danh sách" Click="OnRefreshClicked"/>
                </CommandBar.SecondaryCommands>
            </CommandBar>
        </Grid>

        <controls:DataGrid 
            Grid.Row="2"
            x:Name="ImportHistoryGrid"
            ItemsSource="{x:Bind ViewModel.ImportLogs, Mode=OneWay}"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            GridLinesVisibility="Horizontal"
            DoubleTapped="OnImportRowDoubleTapped">

            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Mã phiếu" Binding="{Binding Id}" Width="2*"/>

                <controls:DataGridTextColumn Header="Ngày lập" 
                    Binding="{Binding CreatedAt, Converter={StaticResource StringFormatConverter}, ConverterParameter='dd/MM/yyyy HH:mm'}" 
                    Width="1.5*"/>

                <controls:DataGridTextColumn Header="Tổng tiền" 
                    Binding="{Binding TotalAmount, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:N0} đ'}" 
                    Width="1.5*"/>

                <controls:DataGridTemplateColumn Header="Trạng thái" Width="1.5*">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <FontIcon 
                                    Glyph="{Binding Status, Converter={StaticResource StatusIconConverter}}" 
                                    Foreground="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                    FontSize="14"/>

                                <TextBlock 
                                    Text="{Binding Status, Converter={StaticResource StatusTextConverter}}"
                                    Foreground="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                    FontWeight="SemiBold"
                                    VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="4">
                <TextBlock Text="Tổng số phiếu:" Foreground="Gray" FontStyle="Italic"/>
                <TextBlock Text="{x:Bind ViewModel.TotalCountText, Mode=OneWay}" Foreground="Gray" FontStyle="Italic" FontWeight="SemiBold"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="20" Height="20" Margin="0,0,8,0"/>

                <Button Content="Trang trước" Command="{x:Bind ViewModel.LoadPreviousPageCommand}" IsEnabled="{x:Bind ViewModel.HasPreviousPage, Mode=OneWay}"/>
                <Button Content="Trang sau" Command="{x:Bind ViewModel.LoadNextPageCommand}" IsEnabled="{x:Bind ViewModel.HasNextPage, Mode=OneWay}" Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>
        </Grid>

    </Grid>
</Page>