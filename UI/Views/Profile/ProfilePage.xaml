<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UI.Views.Profile.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI.Views.Profile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:UI.Utils.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource LayerFillColorDefaultBrush}">
    <Page.Resources>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="32"/>
            <Setter Property="Shadow">
                <Setter.Value>
                    <ThemeShadow />
                </Setter.Value>
            </Setter>
        </Style>

        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Padding="20" MinHeight="600">

            <Border Style="{StaticResource CardBorderStyle}" 
                    MaxWidth="450" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">

                <StackPanel Spacing="24">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE72E;" FontSize="24" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                        <TextBlock Text="Cài đặt bảo mật" 
                                   Style="{StaticResource TitleTextBlockStyle}" 
                                   FontSize="22" 
                                   FontWeight="SemiBold"/>
                    </StackPanel>

                    <TextBlock Text="Thay đổi mật khẩu đăng nhập của bạn để bảo vệ tài khoản tốt hơn." 
                               Style="{StaticResource CaptionTextBlockStyle}" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"/>

                    <MenuFlyoutSeparator Margin="0,-8,0,0"/>

                    <StackPanel Spacing="16">
                        <PasswordBox Header="Mật khẩu hiện tại" 
                                     PasswordChar="●"
                                     Password="{x:Bind ViewModel.CurrentPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     PlaceholderText="Nhập mật khẩu đang dùng"/>

                        <PasswordBox Header="Mật khẩu mới" 
                                     PasswordChar="●"
                                     Password="{x:Bind ViewModel.NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     PlaceholderText="Tối thiểu 6 ký tự"/>

                        <PasswordBox Header="Xác nhận mật khẩu mới" 
                                     PasswordChar="●"
                                     Password="{x:Bind ViewModel.ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     PlaceholderText="Nhập lại mật khẩu mới"/>
                    </StackPanel>

                    <InfoBar Title="Thông báo" 
                             Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                             IsOpen="{x:Bind ViewModel.IsStatusOpen, Mode=TwoWay}"
                             Severity="{x:Bind ViewModel.StatusSeverity, Mode=OneWay}"/>

                    <Grid Margin="0,8,0,0">
                        <Button Content="Cập nhật mật khẩu" 
                                Style="{StaticResource AccentButtonStyle}"
                                Command="{x:Bind ViewModel.ChangePasswordCommand}"
                                IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                                Padding="16,8"
                                HorizontalAlignment="Right"/>

                        <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" 
                                      HorizontalAlignment="Left" 
                                      VerticalAlignment="Center"
                                      Width="24" Height="24"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>