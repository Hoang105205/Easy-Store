<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UI.Views.Statistics.StatisticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI.Views.Statistics"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:converters="using:UI.Utils.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Shadow">
                <Setter.Value>
                    <ThemeShadow />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CaptionTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="CharacterSpacing" Value="50"/>
        </Style>

        <Style x:Key="ProfitColumnStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource DashboardSuccessBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="1.2*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Báo cáo &amp; Thống kê" Style="{StaticResource TitleTextBlockStyle}" FontWeight="Bold"/>
                <TextBlock Text="Theo dõi hiệu suất kinh doanh của cửa hàng" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12" VerticalAlignment="Center">
                <CalendarDatePicker Date="{x:Bind ViewModel.FromDate, Mode=TwoWay}" PlaceholderText="Từ ngày" Width="150"/>
                <CalendarDatePicker Date="{x:Bind ViewModel.ToDate, Mode=TwoWay}" PlaceholderText="Đến ngày" Width="150"/>
                <Button Command="{x:Bind ViewModel.LoadStatisticsCommand}" Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE721;" FontSize="14"/>
                        <TextBlock Text="Cập nhật"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <InfoBar 
                Title="Lỗi chọn ngày"
                Message="{x:Bind ViewModel.DateErrorMessage, Mode=OneWay}"
                IsOpen="{x:Bind ViewModel.IsDateErrorVisible, Mode=TwoWay}"
                Severity="Error"
                IsClosable="True" 
                Margin="0,0,0,16"/>
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="16" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="4">
                    <TextBlock Text="TỔNG DOANH THU" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.TotalRevenueText, Mode=OneWay}" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="{ThemeResource DashboardRevenueBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="4">
                    <TextBlock Text="LỢI NHUẬN THUẦN" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.TotalProfitText, Mode=OneWay}" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="{ThemeResource DashboardSuccessBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="4">
                    <TextBlock Text="TỔNG ĐƠN HÀNG" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.TotalOrders, Mode=OneWay}" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource CardBorderStyle}" BorderBrush="OrangeRed" BorderThickness="1">
                <StackPanel Spacing="4">
                    <TextBlock Text="SẮP HẾT HÀNG" Style="{StaticResource CaptionTextBlockStyle}" Foreground="OrangeRed"/>
                    <TextBlock Text="{x:Bind ViewModel.LowStockCount, Mode=OneWay}" FontSize="24" FontWeight="Bold" Foreground="OrangeRed"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Biểu đồ Doanh thu &amp; Lợi nhuận" FontWeight="SemiBold" Margin="0,0,0,12"/>

                <lvc:CartesianChart
                    Grid.Row="1"
                    Series="{x:Bind ViewModel.Series, Mode=OneWay}"
                    XAxes="{x:Bind ViewModel.XAxes, Mode=OneWay}"
                    YAxes="{x:Bind ViewModel.YAxes, Mode=OneWay}"
                    LegendPosition="Top"
                    LegendTextPaint="{x:Bind ViewModel.LegendTextPaint, Mode=OneWay}"
                    Visibility="{x:Bind ViewModel.HasData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                </lvc:CartesianChart>

                <StackPanel Grid.Row="1" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Spacing="12"
                    Visibility="{x:Bind ViewModel.HasData, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <FontIcon Glyph="&#xE9CE;" FontSize="40" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                    <TextBlock Text="Không có giao dịch nào trong khoảng thời gian này" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Top 5 sản phẩm lợi nhuận cao nhất" FontWeight="SemiBold" Margin="0,0,0,12"/>

                <controls:DataGrid 
                Grid.Row="1"
                ItemsSource="{x:Bind ViewModel.TopProducts, Mode=OneWay}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                GridLinesVisibility="Horizontal">
                    <controls:DataGrid.Columns>
                        <controls:DataGridTextColumn Header="Sản phẩm" Binding="{Binding ProductName}" Width="*"/>
                        <controls:DataGridTextColumn Header="Mã SKU" Binding="{Binding Sku}" Width="Auto"/>
                        <controls:DataGridTextColumn Header="Số lượng" Binding="{Binding QuantitySold}" Width="Auto"/>
                        <controls:DataGridTextColumn Header="Doanh thu" Binding="{Binding Revenue, ConverterParameter='N0', Converter={StaticResource StringFormatConverter}}" Width="Auto"/>
                        <controls:DataGridTextColumn Header="Lợi nhuận" 
                             Binding="{Binding Profit, Converter={StaticResource StringFormatConverter}, ConverterParameter='N0'}" 
                             ElementStyle="{StaticResource ProfitColumnStyle}"
                             Width="Auto"/>
                    </controls:DataGrid.Columns>
                </controls:DataGrid>
            </Grid>
        </Border>

        <Grid Grid.RowSpan="5" Background="#44000000" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing IsActive="True" Width="60" Height="60"/>
        </Grid>
    </Grid>
</Page>
