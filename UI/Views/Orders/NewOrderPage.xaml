<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UI.Views.Orders.NewOrderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI.Views.Orders"
    xmlns:viewmodels="using:UI.ViewModels.Orders"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <TabView x:Name="OrderTabView" 
             TabItemsSource="{x:Bind ViewModel.Tabs, Mode=OneWay}"
             SelectedItem="{x:Bind ViewModel.SelectedTab, Mode=TwoWay}"
             AddTabButtonClick="OrderTabView_AddTabButtonClick"
             TabCloseRequested="OrderTabView_TabCloseRequested"
             IsAddTabButtonVisible="True"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

        <TabView.TabItemTemplate>
            <DataTemplate x:DataType="viewmodels:OrderTabItemModel">
                <TabViewItem Header="{x:Bind Header, Mode=OneWay}">

                    <TabViewItem.IconSource>
                        <SymbolIconSource Symbol="Document" />
                    </TabViewItem.IconSource>

                    <local:OrderTabContentControl />

                </TabViewItem>
            </DataTemplate>
        </TabView.TabItemTemplate>
    </TabView>
</Page>