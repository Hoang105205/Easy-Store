<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UI.Views.Dashboard.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI.Views.Dashboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:converters="using:UI.Utils.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>
        <converters:StatusColorConverter x:Key="StatusColorConverter"/>
        <converters:StatusIconConverter x:Key="StatusIconConverter"/>
        <converters:StatusTextConverter x:Key="StatusTextConverter"/>
    </Page.Resources>

    <ScrollViewer Background="#F8F9FA" Padding="40">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,5">
                <TextBlock Text="Tổng quan doanh thu" FontSize="24" FontWeight="Bold"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock HorizontalAlignment="Left" Margin="5,0,0,0" Text="{x:Bind ViewModel.DateRangeText, Mode=OneWay}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>

                    <toolkit:Segmented Grid.Column="1"
                                       SelectionChanged="Segmented_SelectionChanged"
                                       HorizontalAlignment="Right"
                                       SelectedIndex="0" 
                                       Background="Transparent" 
                                       BorderThickness="0">

                        <toolkit:Segmented.Resources>
                            <SolidColorBrush x:Key="SegmentedItemForeground" Color="#888" />
                            <SolidColorBrush x:Key="SegmentedItemForegroundPointerOver" Color="#666" />
                            <SolidColorBrush x:Key="SegmentedItemForegroundSelected" Color="#555" />
                            <SolidColorBrush x:Key="SegmentedItemForegroundSelectedPointerOver" Color="#444" />
                        </toolkit:Segmented.Resources>

                        <toolkit:Segmented.ItemContainerStyle>
                            <Style TargetType="toolkit:SegmentedItem">
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Margin" Value="0,0,5,0" />
                                <Setter Property="FontSize" Value="15" />
                            </Style>
                        </toolkit:Segmented.ItemContainerStyle>

                        <toolkit:SegmentedItem Content="Tuần qua" Tag="7"/>
                        <toolkit:SegmentedItem Content="Tháng qua" Tag="30"/>
                        <toolkit:SegmentedItem Content="Năm qua" Tag="365"/>
                        <toolkit:SegmentedItem Content="Toàn bộ" Tag="All"/>
                    </toolkit:Segmented>
                </Grid>
                
                <Grid ColumnSpacing="30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="TỔNG DOANH THU" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind ViewModel.Stats.TotalIncome, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:N0} đ'}" Style="{StaticResource TitleTextBlockStyle}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="TỔNG ĐƠN HÀNG MỚI" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind ViewModel.Stats.TotalNewOrders, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:N0}'}" Style="{StaticResource TitleTextBlockStyle}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>

            <Grid Grid.Row="1" ColumnSpacing="30" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" Padding="16"
                        MaxHeight="400"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1">
                    <Grid RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Text="Sản phẩm sắp hết hàng" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13" FontWeight="SemiBold"/>

                        <ScrollViewer Grid.Row="1" Padding="20">
                            <ItemsControl ItemsSource="{x:Bind ViewModel.Stats.NearlyOutOfStock, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{ThemeResource BackgroundBrush}" CornerRadius="6" Padding="15,10" Margin="0,0,0,10">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid Tapped="ToggleDetails_Tapped" Background="Transparent">
                                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Foreground="#444"/>
                                                    <FontIcon Glyph="&#xE70D;" FontSize="12" Foreground="#444" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                                </Grid>
                                                <StackPanel Visibility="Collapsed" Grid.Row="1" Margin="0,7,0,0" Orientation="Vertical">
                                                    <TextBlock Text="{Binding Quantity, Converter={StaticResource StringFormatConverter}, ConverterParameter='Số lượng còn: {0}'}" FontSize="12" Foreground="#444"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <Border Grid.Column="1" 
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="8" Padding="16" 
                        MaxHeight="400"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1">
                    <Grid RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Text="Sản phẩm bán chạy" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                   FontSize="13" FontWeight="SemiBold"/>
                        
                        <ScrollViewer Grid.Row="1" Padding="20">
                            <ItemsControl ItemsSource="{x:Bind ViewModel.Stats.BestSellingProducts, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{ThemeResource BackgroundBrush}" CornerRadius="6" Padding="15,10" Margin="0,0,0,10">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid Tapped="ToggleDetails_Tapped" Background="Transparent">
                                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Foreground="#555"/>
                                                    <FontIcon Glyph="&#xE70D;" FontSize="12" Foreground="#444" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                                </Grid>
                                                <StackPanel Visibility="Collapsed" Grid.Row="1" Margin="0,7,0,0" Orientation="Vertical">
                                                    <TextBlock Text="{Binding Quantity, Converter={StaticResource StringFormatConverter}, ConverterParameter='Số lượng đặt: {0}'}" FontSize="12" Foreground="#444"/>
                                                    <TextBlock Text="{Binding LastOrder, Converter={StaticResource StringFormatConverter}, ConverterParameter='Lần đặt hàng gần nhất: {0:dd/MM/yyyy HH:mm}'}" FontSize="12" Foreground="#444"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            CornerRadius="12" Padding="30" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Biểu đồ doanh thu" FontSize="20" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock Text="(Đơn vị: Nghìn VNĐ)" FontSize="12" Foreground="#888" Margin="0,4,0,15"/>

                    <Grid Height="300">
                        <lvc:CartesianChart 
                            Series="{x:Bind ViewModel.RevenueSeries, Mode=OneWay}" 
                            XAxes="{x:Bind ViewModel.XAxes, Mode=OneWay}" 
                            YAxes="{x:Bind ViewModel.YAxes, Mode=OneWay}"
                            TooltipPosition="Top"
                            TooltipTextSize="12"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
